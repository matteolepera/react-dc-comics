import LoadButtonStyle from "../components/LoadButton.module.css"
export default function LoadButton({ title }) {
    return (
        <button className={LoadButtonStyle.a} href="">{title}</button>
    )

}